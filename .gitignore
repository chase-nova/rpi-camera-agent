# python
*.pyc
__pycache__/
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# stage env files — only .env.example is committed (tokens live in these)
.env
.env.*
!.env.example

# device credential, local captures and the upload queue
credential.json
captures/
queue/
